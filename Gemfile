source 'https://rubygems.org'

# I like Pry, and use it as my development console. Give it a try!
gem 'pry' 

gem 'rdf'

# `linkeddata` is a meta-distribution of RDF libraries. It supports a wide range
# of serialization formats, isomorphism, a reasoner, and SPARQL (server & 
# client)
gem 'linkeddata' 
gem 'rdf-yoda_triples', github: 'no-reply/rdf-yoda_triples'

gem 'sinatra'
